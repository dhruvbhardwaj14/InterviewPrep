full binary tree-> every node has 0 or 2 childrens
complete binary tree-> all levels are completely filled except the last level and last level has all nodes as left as possible
perfect binary tree-> all leaves are at same level
balanced binary tree-> height of tree at max log(n)
degenerate tree-> skew tree