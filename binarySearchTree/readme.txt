binary search tree-> a type of binary tree in which for all nodes
-> left child should be lesser than root
-> right child should be greater than root

why bst?->  as in BT search takes O(n) time while in BST search takes Log(n) time